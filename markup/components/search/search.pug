mixin search(data)
    .search
        form.search__form(action="")
            input.search__input(type="text" name="" placeholder="Поиск")
            button.search__button(type="submit" name=""  placeholder="")
